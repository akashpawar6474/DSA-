class Solution:
    # @param A : integer
    # @return an integer
    def solve(self, A):
        """
        tc = O(n)
        sc = O(n)
        """
        if A <= 1:
            return 1
        return A * self.solve(A - 1)
